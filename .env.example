# Supabase — frontend (exposed to browser via VITE_ prefix)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase — server-only (used in /api/ serverless functions)
SUPABASE_SERVICE_ROLE_KEY=

# Google AI — ingestion pipeline scripts only (free tier, not used in production)
# Get a free key at https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=
